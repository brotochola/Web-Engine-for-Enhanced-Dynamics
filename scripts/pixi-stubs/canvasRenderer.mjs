/** Stub: weed Pixi worker is WebGL-only. autoDetect must never instantiate this. */
export class CanvasRenderer {
  constructor() {
    throw new Error('Canvas renderer stripped from weed Pixi worker bundle');
  }
}
